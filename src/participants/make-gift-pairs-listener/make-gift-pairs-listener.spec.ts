import { MakeGiftPairsListener } from './make-gift-pairs-listener';

describe('MakeGiftPairsListener', () => {
  it('should be defined', () => {
    //expect(new MakeGiftPairsListener()).toBeDefined();
  });
});
